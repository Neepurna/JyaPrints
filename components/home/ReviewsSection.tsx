import { reviews } from "@/lib/data/reviews";import { SectionLabel } from "@/components/shared/SectionLabel";import { ArrowLink } from "@/components/shared/ArrowLink";import { StarRating } from "@/components/shared/StarRating";import { AnimatedReveal } from "@/components/shared/AnimatedReveal";
export function ReviewsSection(){return <AnimatedReveal><section className="section"><div className="section-head"><SectionLabel>REVIEWS</SectionLabel><ArrowLink href="/reviews">See all reviews</ArrowLink></div><div className="review-grid">{reviews.slice(0,3).map((r,i)=><article className="review-card" key={r.name}><span className="quote-no">0{i+1}</span><StarRating/><blockquote>“{r.quote}”</blockquote><div className="reviewer"><span>{r.name.slice(0,1)}</span><div><b>{r.name}</b><small>{r.role}</small></div></div></article>)}</div></section></AnimatedReveal>}
