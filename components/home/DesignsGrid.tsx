import { products } from "@/lib/data/products";import { ProductCard } from "@/components/product/ProductCard";import { SectionLabel } from "@/components/shared/SectionLabel";import { ArrowLink } from "@/components/shared/ArrowLink";import { AnimatedReveal } from "@/components/shared/AnimatedReveal";
export function DesignsGrid(){return <AnimatedReveal><section className="section designs"><div className="section-head"><SectionLabel>DESIGNS</SectionLabel><ArrowLink href="/designs">View all designs</ArrowLink></div><div className="product-grid">{products.map((p,i)=><ProductCard product={p} index={i} key={p.id}/>)}</div></section></AnimatedReveal>}
