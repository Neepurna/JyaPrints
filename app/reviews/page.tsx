import type {Metadata} from "next";import {reviews} from "@/lib/data/reviews";import {SectionLabel} from "@/components/shared/SectionLabel";import {StarRating} from "@/components/shared/StarRating";
export const metadata:Metadata={title:"Reviews"};export default function Reviews(){return <div className="page"><header className="page-hero"><SectionLabel>COMMUNITY NOTES</SectionLabel><h1>WORN.<br/><span>SPOKEN.</span></h1><p>Unfiltered words from the people carrying JYA into the street.</p></header><section className="all-reviews">{reviews.map((r,i)=><article key={r.name}><span>0{i+1}</span><StarRating/><blockquote>“{r.quote}”</blockquote><div><b>{r.name}</b><small>{r.role}</small></div></article>)}</section></div>}
