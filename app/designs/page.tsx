import type {Metadata} from "next";import {products} from "@/lib/data/products";import {ProductCard} from "@/components/product/ProductCard";import {SectionLabel} from "@/components/shared/SectionLabel";
export const metadata:Metadata={title:"Designs"};export default function Designs(){return <div className="page"><header className="page-hero"><SectionLabel>THE CURRENT DROP</SectionLabel><h1>DESIGNS THAT<br/><span>DON’T WHISPER.</span></h1><p>Original artwork. Small batches. Once it is gone, it becomes archive.</p></header><div className="filter-row"><b>DROP 01 / 2026</b><span>04 PIECES</span><span>ALL DESIGNS ↓</span></div><section className="shop-grid">{products.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</section></div>}
