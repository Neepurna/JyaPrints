import {notFound} from "next/navigation";import Image from "next/image";import Link from "next/link";import {products,getProduct} from "@/lib/data/products";import {formatPrice} from "@/lib/utils";import {ProductActions} from "./product-actions";
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProduct(slug);if(!p)notFound();return <div className="product-page"><div className="product-gallery"><Link href="/designs" className="back-link">← ALL DESIGNS</Link><Image src={p.image} alt={p.name} fill priority sizes="(min-width:900px) 60vw, 100vw"/></div><div className="product-info"><p>DROP 01 / PIECE {p.id}</p>{p.tag&&<span className="new-tag static">{p.tag}</span>}<h1>{p.name}</h1><h2>{formatPrice(p.price)}</h2><p className="story">{p.story}</p><div className="spec-line"><span>240 GSM COTTON</span><span>OVERSIZED FIT</span><span>DTG PRINT</span></div><ProductActions product={p}/><div className="detail-notes"><p><b>LIMITED BY DESIGN.</b><br/>No reprints once the drop sells through.</p><p><b>MADE TO MOVE.</b><br/>Pre-shrunk, heavyweight cotton built for daily wear.</p></div></div></div>}
